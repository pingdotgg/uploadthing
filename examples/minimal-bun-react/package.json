{
  "name": "@examples/bun-react",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"bun --cwd client dev\" \"bun --cwd server dev\"",
    "build": "bun --cwd client build && bun --cwd server build"
  },
  "devDependencies": {
    "concurrently": "^8.2.1",
    "typescript": "^5.1.6"
  }
}
