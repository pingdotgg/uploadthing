{
  "name": "@example/minimal-hono-react",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"bun run --cwd client dev\" \"bun run --cwd server dev\"",
    "build": "bun run --cwd client build && bun run --cwd server build",
    "start": "concurrently \"bun run --cwd client start\" \"bun run --cwd server start\""
  },
  "devDependencies": {
    "@uploadthing/react": "5.6.0",
    "concurrently": "^8.2.1",
    "typescript": "^5.1.6",
    "uploadthing": "^5.6.0"
  }
}
