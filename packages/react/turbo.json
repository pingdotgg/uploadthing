{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["!NODE_ENV"],
      "outputs": ["dist/**", "native/**", "next-ssr-plugin/**"]
    }
  }
}
